datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model UserLanguage {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  languages String
  age       Int
}